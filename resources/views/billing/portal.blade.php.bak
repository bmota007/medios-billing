@extends('layouts.app')

@section('content')

<div class="max-w-5xl mx-auto py-10 px-4 md:px-6">

    <div class="rounded-3xl p-8 md:p-10 border border-white/10 shadow-2xl"
         style="background:linear-gradient(135deg,#0b1220,#0f172a,#111827);">

        <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-6 mb-10">

            <div>
                <div class="inline-block px-4 py-2 rounded-full text-xs font-bold tracking-wider mb-4"
                     style="background:rgba(59,130,246,.15);color:#60a5fa;">
                    BILLING CENTER
                </div>

                <h1 class="text-4xl md:text-5xl font-bold text-white leading-tight">
                    Subscription Management
                </h1>

                <p class="text-slate-400 mt-3 text-lg">
                    Manage your Medios Billing plan securely anytime.
                </p>
            </div>

            <div class="text-right">
                <div class="text-sm text-slate-400">Powered by</div>
                <div class="text-2xl font-bold text-sky-400">
                    Stripe Secure Billing
                </div>
            </div>

        </div>

        @if(session('success'))
            <div class="mb-6 rounded-2xl p-4 bg-green-500/10 border border-green-500/20 text-green-300">
                {{ session('success') }}
            </div>
        @endif

        @if(session('error'))
            <div class="mb-6 rounded-2xl p-4 bg-red-500/10 border border-red-500/20 text-red-300">
                {{ session('error') }}
            </div>
        @endif

        <div class="grid md:grid-cols-2 xl:grid-cols-4 gap-5 mb-10">

            <div class="rounded-2xl p-6 border border-white/10 bg-white/5">
                <div class="text-sm text-slate-400">Current Plan</div>
                <div class="text-3xl font-bold text-white mt-2">
{{ optional($company)->plan_name ?? 'Starter' }}
                </div>
            </div>

            <div class="rounded-2xl p-6 border border-white/10 bg-white/5">
                <div class="text-sm text-slate-400">Monthly Price</div>
                <div class="text-3xl font-bold text-green-400 mt-2">
{{ number_format(optional($company)->monthly_price ?? 49, 2) }}
                </div>
            </div>

            <div class="rounded-2xl p-6 border border-white/10 bg-white/5">
                <div class="text-sm text-slate-400">Status</div>
                <div class="text-2xl font-bold text-sky-400 mt-2">
{{ ucfirst(optional($company)->subscription_status ?? 'trialing') }}
                </div>
            </div>

            <div class="rounded-2xl p-6 border border-white/10 bg-white/5">
                <div class="text-sm text-slate-400">Trial Ends</div>
                <div class="text-xl font-bold text-amber-300 mt-2">
{{ optional($company)->trial_ends_at ? optional($company->trial_ends_at)->format('F d, Y') : 'N/A' }}
                </div>
            </div>

        </div>

        <div class="rounded-2xl p-7 border border-blue-500/20 mb-10"
             style="background:rgba(59,130,246,.08);">

            <h3 class="text-2xl font-bold text-white mb-4">
                Included With Your Subscription
            </h3>

            <div class="grid md:grid-cols-2 gap-4 text-slate-300">

                <div>✓ Unlimited Invoices</div>
                <div>✓ Unlimited Quotes</div>
                <div>✓ Client Payment Portal</div>
                <div>✓ Stripe Payment Collection</div>
                <div>✓ Customer Database</div>
                <div>✓ Business Growth Tools</div>

            </div>
        </div>

        <div class="flex flex-wrap gap-4">

            <form method="POST" action="{{ route('subscription.cancel') }}">
                @csrf

                <button
                    onclick="return confirm('Are you sure you want to cancel your subscription?')"
                    class="px-7 py-4 rounded-2xl font-bold text-white bg-red-600 hover:bg-red-700 transition">
                    Cancel Subscription
                </button>
            </form>

            <a href="/dashboard"
               class="px-7 py-4 rounded-2xl font-bold text-white bg-blue-600 hover:bg-blue-700 transition">
               Back to Dashboard
            </a>

        </div>

        <div class="mt-8 text-sm text-slate-500">
            You may cancel anytime before your next billing cycle.
        </div>

    </div>

</div>

@endsection
